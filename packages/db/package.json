{
  "name": "@roler/db",
  "type": "module",
  "version": "0.1.0",
  "private": true,
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "lint": "eslint src --ext .ts",
    "typecheck": "tsc --noEmit",
    "test": "vitest --run --coverage",
    "test:watch": "vitest --watch",
    "test:changed": "vitest --changed --coverage",
    "test:ci": "vitest --run --reporter=dot --coverage"
  },
  "devDependencies": {
    "typescript": "^5.6.3",
    "vitest": "^2.1.9"
  }
}
